# פריז 2026 — אפליקציית טיול משפחתית

אתר סטטי (קובץ `index.html` יחיד) המשמש משפחה ישראלית כאפליקציית טיול בפריז, 27.7–4.8.2026. מתארח ב-GitHub Pages, מותקן כ-PWA על טלפוני המשפחה.

## הקשר המשפחה והטיול — אל תשבור את האילוצים האלה

- 2 מבוגרים + ילדים בני 8 ו-11. דירה: **8 Rue de l'Arcade, 75008** (מדלן). כל המרחקים, התחנות והמסלולים בקוד מחושבים מהכתובת הזו (קבוע `HOME`).
- **גל חום**: העיקרון המארגן של כל הלו"ז — יציאה מוקדמת, מנוחה במיזוג ~12:30–19:00 בימים חמים, ערבים בחוץ. אין להעמיס פעילות בשעות השיא בימים של 33°+.
- **שומרי שבת**: שישי בערב ושבת (1.8) ללא נסיעות וללא קניות. קניות לשבת מסתיימות עד צהרי שישי (Monoprix, 47 Bd Malesherbes). שבת = יום הליכה בלבד סביב הדירה.
- **טיסות אל על LY316**: הלוך 28.7 05:40→09:30 CDG (יציאה מצור הדסה 01:30); חזור 4.8 17:35→22:55 (יציאה מהדירה 13:15).
- **כרטיסים קנויים (שעות קבועות — לא להזיז):** לובר חמישי 30.7 ב-15:00 (המשתמש אישר: "הלובר בחמישי זו עובדה"; בחמישי המוזיאון נסגר ~18:00). ורסאי ראשון 2.8 ב-13:30, הגעה 13:00, כרטיס גישה מלאה כולל גנים וטריאנון.
- **החלטות מפורשות של המשפחה:** ערב חמישי = שער הניצחון + שאנז + חנות PSG (חובה, בערב דווקא). מונמרט = ערב (מוצע בהגעה וכבחירה בערב האחרון). אייפל = בחירה לשני 3.8 אחה"צ (סיכון סופות → המוזיאון הוא גיבוי). אטרקציה שדולגה — לא משלימים ביום אחר.
- תיקיית מסמכים משפחתית ב-Google Drive מוטמעת בקוד (קבוע `DEFAULT_DOCS`).

## ארכיטקטורה

- `index.html` — כל האפליקציה: CSS + HTML + JS בקובץ אחד, עברית RTL, עיצוב בהשראת שילוט מטרו פריזאי (כחול `--metro: #0B2E6B`, צ'יפים בצבעי הקווים האמיתיים).
- `sw.js` — service worker, cache-first, לעבודה אופליין. `manifest.json` + אייקונים — PWA.
- אין build, אין תלויות, אין שרת. פונטים מ-Google Fonts (Secular One / Assistant).

### מבני נתונים ב-`index.html`

- `DAYS[]` — 9 ימים (`d0` התארגנות עד `d8` חזרה). כל יום: `date` (מפעיל זיהוי "היום" אוטומטי), `temp`, `note`, `fix` (התראה כחולה), `stops[]`.
- תחנה: `{t, h, p, chips, m, dir, dirs, rest, aik}` — `m` = שאילתת Google Maps; `dir:true` מוסיף מסלול הליכה מ-`HOME`; `dirs` = מסלולים מותאמים `[טקסט, מוצא, יעד, אמצעי]`; `rest:true` = עיצוב תחנת מנוחה.
- תחנת בחירה: `{t, key, h, choice:[opt,opt]}` — הבחירה נשמרת לפי `key`.
- `AIBG{}` — מאגר "תעלומה בזמן": לכל אתר רקע היסטורי אמיתי שמוטמע בפרומפט מוכן להעתקה (פורמט ציד-מטמון לילדים בסגנון "תעלומות בזמן" של בית אביחי). מפתח = ערך `m` של התחנה, או `aik` מפורש. ערכים נושאיים: `METRO, CAFE, SHOP, FLY, REST, PREP, CDG`. ערכים עם `raw:true` מנסחים את המיקום בעצמם.
- `CHECKLIST[]` — משימות/כרטיסים עם סטטוס (`have/buy/opt`) וקישורי הזמנה.

### אחסון — אל תשנה שמות מפתחות

`paris26-checklist`, `paris26-choices`, `paris26-places`, `paris26-docslink` — נשמרים דרך `sGet/sSet` (window.storage אם קיים, אחרת localStorage). שינוי שם מפתח ימחוק למשתמשים את הסימונים באמצע הטיול.

## מוסכמות עדכון ופריסה

1. עריכות תוכן — רק ב-`index.html`. לשמור על עברית, RTL, ומינימום פורמט קיים.
2. **בכל פריסה: להעלות את גרסת המטמון ב-`sw.js`** (`paris26-v1` → `v2` וכו'), אחרת המשתמשים יראו גרסה ישנה.
3. תחנה חדשה עם אתר משמעותי → להוסיף ערך `AIBG` עם רקע היסטורי **מאומת בלבד** (בלי להמציא עובדות), או `aik` לערך נושאי קיים.
4. `git push` ל-main פורס אוטומטית ל-GitHub Pages.
5. לבדוק לפני פריסה: תחביר JS תקין, `todayIndex()` עדיין מזהה תאריכים, והדף נטען ללא רשת (אופליין) אחרי טעינה ראשונה.

## מה לא לעשות

- לא להוסיף תלויות, frameworks או קבצים נוספים ללא צורך ממשי.
- לא לשנות את לוגיקת השבת, שעות הכרטיסים הקנויים, או עקרון המנוחה בצהריים.
- לא להסיר את תג `noindex` (הריפו ציבורי; הלו"ז חושף תאריכי היעדרות מהבית).
- לא לגעת במפתחות האחסון.
